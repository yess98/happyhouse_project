<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.GuestBookMapper">

	<insert id="registerArticle" parameterType="GuestBookDto">
		insert into notice (user_id, subject, contents)
		values (#{user_id}, #{subject}, #{contents})
		<selectKey resultType="int" keyProperty="no" order="AFTER">
			select last_insert_id()
		</selectKey>
	</insert>

	<resultMap type="GuestBookDto" id="articleList">
		<result column="no" property="no"/>
		<result column="user_id" property="user_id"/>
		<result column="subject" property="subject"/>
		<result column="contents" property="contents"/>
	</resultMap>

	<sql id="search">
		<if test="word != null and word != ''">
			<if test="key == subject">
				and subject like concat('%', #{word}, '%')
			</if>
			<if test="key != subject">
				and ${key} = #{word}
			</if>
		</if>
	</sql>

	<select id="listArticle" parameterType="map" resultMap="articleList">
		select g.no, g.user_id, g.subject, g.contents
		from notice g
		where g.user_id = "ssafy"
		<include refid="search"></include>
		order by g.no desc
		limit #{start}, #{spp}
	</select>
	
	
	<select id="getTotalCount" parameterType="map" resultType="int">
		select count(no)
		from notice
		<where>
			<include refid="search"></include>
		</where>
	</select>
	
	<select id="getArticle" parameterType="int" resultType="GuestBookDto">
		select *
		from notice
		where no = #{no}
	</select>
	
	<update id="updateArticle" parameterType="GuestBookDto">
		update notice
		set subject = #{subject}, contents = #{contents}
		where no = #{no}
	</update>
	
	
	<delete id="deleteArticle" parameterType="int">
		delete from notice
		where no = #{no}
	</delete>

</mapper>
